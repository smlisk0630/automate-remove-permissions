# automate-remove-permissions
Package that automatically removes permission sets, permission set licenses, permission set groups, queues, and public groups from user record upon deactivation of the user. 
